@model DAIF2020.Models.SettingModels.PersonRole

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>PersonRole</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PersonRoleName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PersonRoleName)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-sm btn-success" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
    <a class="btn btn-sm btn-success" asp-action="ListPersonRoles">Back to person roles</a>
</div>
