@model DAIF2020.Models.ViewModels.SettingsViewModel
@{
    ViewData["Title"] = "ListReceiptTypes";
}

<h1>ListReceipt Types</h1>

<a class="btn btn-sm btn-success" asp-controller="ReceiptTypes" asp-action="Create">Create new type</a>

<table class="table table-responsive-sm table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReceiptTypes[0].ReceiptTypeName)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ReceiptTypes.OrderBy(r => r.ReceiptTypeName))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ReceiptTypeName)
                </td>

                <td>
                    <a class="btn btn-sm btn-success" asp-controller="ReceiptTypes" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-sm btn-success" asp-controller="ReceiptTypes" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-sm btn-danger" asp-controller="ReceiptTypes" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>